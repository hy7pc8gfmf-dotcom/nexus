add_executable(algo_engine main.cpp)
target_link_libraries(algo_engine PRIVATE nexus_ipc)
if(NEXUS_USE_VCPKG)
  target_link_libraries(algo_engine PRIVATE fmt::fmt spdlog::spdlog)
endif()
install(TARGETS algo_engine RUNTIME DESTINATION bin)
